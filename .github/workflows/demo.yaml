name: Push Update


on:
   push:
 
 
jobs:
  clone_public_repo:
    name: push to repo with token
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update log
        run: echo "Last updated on $(date)" > update.log

      - name: Commit and Push
        run: |
          git config --global user.email "bakiev_bai@mail.ru"
          git config --global user.name "ara-bakiev997"
          git add update.log
          git commit -m "update.log"
          git push 
